{
  "name": "ket-quality-tools",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check:format": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "3.8.4"
  }
}
